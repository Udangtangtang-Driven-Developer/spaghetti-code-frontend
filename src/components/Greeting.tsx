import React from "react";

const Greeting = () => {
  return (
    <div>
      Hello, World!
      <button onClick={() => alert("wow!!!")}>click</button>
    </div>
  );
};

export default Greeting;
